/***
 * Test program for simple arithmetic.
 */
 

def main(){
	let x = 3;
	let a = x + 4;
	let b = x - 4;
	let c = x * 4;
	let d = 4 / 2;
	let e = 4 % 2;
	let f = x ** 2;

	return [x, a, b, c, d, e, f];
}
