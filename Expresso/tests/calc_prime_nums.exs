/***
 * This is a performance test for Expresso
 */
 
 
//require math;
 
def main(){
	let n = 100000, i = 2/*, results = []*/;
	let j (- int;
	while(i <= n){
		let SQRT_I = toInt(sqrt(i) + 1.0);
		j = 2;
		while(j <= SQRT_I){
			if(j == SQRT_I)
				print i;
			else if(i % j == 0)
				break;

			j = j + 1;
		}
		i = i + 1;
	}
	
	/*print "Results: ";
	for(j in results){
		print j;
	}*/
}
